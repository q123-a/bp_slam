"""
GNN 预热策略说明
================================

## 什么是预热（Warmup）？

预热是指在 SLAM 循环的前 N 步中：
- GNN 继续训练（更新权重）
- 但不使用 GNN 的输出
- 使用纯 BP 的结果进行数据关联

代码位置：slam.py:584
```python
if step > warmup_steps:
    use_gnn_result = True  # 预热结束后才使用 GNN
```

---

## 为什么需要预热？

### 自监督学习（原来的方式）

**需要较长预热（50步）**：
- ❌ 初始权重随机，输出不可靠
- ❌ 自监督信号弱（RSS 一致性、几何匹配）
- ❌ 依赖 BP 预测生成伪标签
- ❌ BP 需要时间稳定，否则伪标签不准

### 监督学习（现在的方式）

**只需要短暂预热（5-10步）**：
- ✅ 标签 100% 准确（不依赖 BP）
- ✅ 监督信号强，收敛快
- ✅ 只需要让 BP 初始化锚点
- ✅ GNN 可以快速学到正确模式

---

## 推荐的预热步数

| 模式 | 预热步数 | 说明 |
|------|---------|------|
| 自监督学习 | 50-100 | 需要较长时间让 BP 稳定 |
| 监督学习 | 5-10 | 只需要让 BP 初始化 |
| 加载预训练权重 | 0-5 | 权重已经训练好，几乎不需要预热 |

---

## 如何设置预热步数

### 方法 1：在 testbed.py 中设置

```python
# testbed.py 中的参数设置部分
if use_gnn:
    # 监督学习模式：减少预热
    parameters['gnn_warmup_steps'] = 10  # 推荐值
```

### 方法 2：通过命令行参数

```bash
python testbed.py \
    --mode gnn \
    --steps 900 \
    --particles 100000 \
    --load-measurements measurement1500.mat \
    --add-clutter \
    --use-sparse-graph-v2 \
    --warmup 10  # 设置预热步数
```

---

## 预热步数的影响

### 预热太长（如 50 步）

**优点**：
- ✅ 更安全，不会影响 BP 初始化
- ✅ GNN 有更多时间学习

**缺点**：
- ❌ 浪费了监督学习的优势
- ❌ 前 50 步没有利用 GNN 的能力
- ❌ 收敛更慢

### 预热太短（如 0 步）

**优点**：
- ✅ 立即使用 GNN
- ✅ 充分利用监督学习优势

**缺点**：
- ❌ 第一步 GNN 输出可能很差（随机权重）
- ❌ 可能误导 BP 的初始化
- ❌ 可能导致早期性能下降

### 预热适中（如 10 步）✅ 推荐

**优点**：
- ✅ 给 BP 足够时间初始化
- ✅ GNN 快速学习后立即使用
- ✅ 平衡了安全性和效率

**缺点**：
- 无明显缺点

---

## 实验建议

### 测试不同预热步数的效果

```bash
# 测试 1: 无预热
python testbed.py --mode gnn --warmup 0 --use-sparse-graph-v2

# 测试 2: 短预热（推荐）
python testbed.py --mode gnn --warmup 10 --use-sparse-graph-v2

# 测试 3: 中等预热
python testbed.py --mode gnn --warmup 25 --use-sparse-graph-v2

# 测试 4: 长预热（原来的设置）
python testbed.py --mode gnn --warmup 50 --use-sparse-graph-v2
```

**观察指标**：
- 位置误差（Position error）
- 锚点估计数量
- 损失值（Loss）
- 收敛速度

---

## 总结

**使用监督学习时的最佳实践**：

1. ✅ **推荐预热步数：10 步**
   - 平衡了安全性和效率
   - 给 BP 足够时间初始化
   - GNN 快速学习后立即使用

2. ✅ **激进方案：5 步**
   - 如果对 GNN 有信心
   - 想要最快收敛

3. ✅ **保守方案：25 步**
   - 如果担心初始不稳定
   - 想要更安全的启动

4. ❌ **不推荐：50 步**
   - 浪费了监督学习的优势
   - 除非使用自监督学习

**关键点**：
- 监督学习不需要长时间预热
- 10 步是一个很好的平衡点
- 可以根据实验结果调整
"""
